# 开发记录

## 当前实现

主目录使用 0.4.7.1 测试包的四个程序文件，保持字节不变。0.4.8 F7 接管源码单独保存在 experimental 中，未完成 Windows 编译及游戏实测。

引导线控制修改两个经校验的方法返回值；写入后验证并恢复内存保护。自动游玩读取进程并经 SendInput 发送空格。
引导点通过事件订阅对象发现，队列遍历替代早期固定深度扫描，扫描受预算约束。
首次识别以引导点所属关卡为入口，实际开始后绑定匹配的玩家。
世界坐标按此版本 Unity Transform 的父级位置、旋转和缩放计算，不调用游戏内部函数。
位置触发检查前向距离、横向偏差、时间窗口及运动状态；落地等待、传送疑似检测与对齐等待均保留待执行点。

Steam 版源码位于 `steam/`，当前为 0.2.9。该版本的 `GameCharacter+0x30` 在刚进关时可能尚未绑定 `LevelBase`，因此采用双路径：先使用角色引用，失败时从已经验证的关卡注册对象（`DancingLine.Audio.mg` 或 `DancingLine.ft`）取得当前关卡。此方案只读取游戏进程内存；不采用曾导致进关闪退的订阅后补发跳板。

## 已知限制

外部读内存不是原子快照。键盘输入跨线程、跨帧，调度延迟会影响转向。
引导点不一定就是道路几何中心。当前位置模式不是地图寻路器。
传送检测为启发式，短距离传送或动态路径可能漏判。对齐等待有超时。
地面/跳跃逻辑和所有偏移仅针对校验通过的游戏二进制。

## 后续

1. 用户已确认现有停止/开始按钮可在手动回主路后继续；0.4.8 F7 快捷版待测试。
2. 单独研究数学关卡路线外钻石；不得把最近钻石位置直接当作安全路线。
3. 增加有针对性的 Windows 编译检查和场景回归。

提交源代码不包含游戏二进制与运行日志。参考过 EricWayne 引导线模组的思路，本工具不代表该作者发布。

## 本轮记录（2026-09-16）

数学约 122 秒的钻石位于引导路线外。用户录像展示手动取得后回归主路，但未提供可直接用于重放的世界坐标或精确内部时间。自动绕行、手动路线录制均未实现，按用户要求暂缓。视频和截图不随源码上传。

西部世界存在早期引导点与角色路线不对齐并超时的问题，未解决；不得将此前其他关卡的通关反馈记为此问题修复。

## 落地漏转修复（0.4.7.1）

日志 index=234 在 clock=135.2834 落地，lateral=-1.0083846，因超过原 1.0 上限持续等待，之后走出平台。没有 LANDING PASS，因此不是直行落点误跳过。修复只在当前落地点的 0.5 秒窗口内允许最大 1.05 横向偏差；用户已确认通关。此反馈只验证该次场景，不保证所有关卡。
