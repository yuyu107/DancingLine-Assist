# DancingLine Community Assist

跳舞的线社区版辅助工具，当前版本 **0.4.5（实验版）**。

提供引导线控制和基于引导点世界坐标的自动转向。仅适配本次测试的 Windows 64 位社区版，不是官方项目，也不保证适配其他版本或所有关卡。

## 使用

1. 下载本仓库 ZIP 并完整解压，保留源码与启动脚本在同一目录。
2. 启动游戏，运行 `Start.cmd`。需要 Windows 自带的 64 位 Windows PowerShell。
3. 点击“启用免费引导线”，进入关卡后点击“识别当前关卡”。首次识别不再要求故意死亡。
4. 点击“启动自动游玩”，切回游戏并手动开始。
5. **F8** 或窗口内停止按钮停止自动点击；关闭窗口也会释放按键。
6. 失败时点击“导出运行日志”，日志保存在工具目录。

测试时使用：游戏内“自动调整”和“自动修正音频输出延迟”开启，工具窗口偏移 **-50 毫秒**。这是已测试配置，不保证适用于全部关卡。

窗口偏移只移动时间校验窗口；当前版本主要根据位置触发空格，不是单纯按时间表点击。

## 功能与边界

- 引导线：启用免费引导线、禁用许可、恢复原规则；修改运行中内存，不修改磁盘上的游戏 DLL。重启游戏后补丁失效。
- 自动转向：读取引导点、计算 Transform 世界坐标，接近目标时发送空格。
- 飞跃：等待落地再判断；不支持任意空中转向。
- 传送：异常位移短暂等待，以及时间窗口内等待路线重新对齐；属于启发式处理。
- 换关：发现关卡数据变化后禁用旧时间表；新关卡必须重新识别。
- 恢复原规则只恢复引导线，不等于停止自动游玩。

**尚未实现**：引导线外钻石的自动收集、可靠的手动接管/自动恢复快捷切换、直接坐标纠偏。当前不会为钻石规划绕路，也不会把角色强制拉回路线。

F8 是停止键，不是无缝手动接管开关。中途恢复的路线衔接尚未验证。

## 测试记录

| 关卡 | 已确认结果 |
| --- | --- |
| 钢琴 | 早期时间模式用户实测通关 |
| 数学 | 0.4.3 位置模式用户实测通关 |
| 足球 | 0.4.5 等待对齐逻辑用户实测通关 |

以上是对话中用户的实测反馈，不代表本版本已在每关重复回归或保证成功。开发环境未运行 Windows 游戏；新关卡、动态路线、窄路等仍可能失败。

## 版本校验

程序校验 `GameAssembly.dll` SHA-256：

```text
be1a8c04fb5507617818054d5521245f60be2f780a8c2eb0a1d3cf488a670094
```

世界坐标读取还校验 `UnityPlayer.dll` SHA-256：

```text
42e16d65341f4036ca6959e5dad65ae48fb94f464ec77c680e71c3fed5ac2ce2
```

不匹配时不要通过删除校验来强行运行，需要重新适配。

## 文件

- `AutoPlayer.cs`：扫描、位置计算、自动输入及诊断。
- `HintMemory.cs`：带版本和字节校验的引导线内存补丁。
- `Start.ps1` / `Start.cmd`：Windows 窗口和启动入口；启动时通过 Add-Type 编译 C#。
- [开发记录](docs/DEVELOPMENT.md)：实现思路、限制与后续工作。
- [更新记录](CHANGELOG.md)：主要里程碑。

日志可能包含进程地址和路径，默认忽略，不随源码提交。仓库不包含游戏程序、DLL、资源、存档或第三方模组安装包。
