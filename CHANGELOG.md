# 更新记录

## 0.4.36 - 正式整合版

- 整合社区版路线修正、遮罩进度显示和进度续玩逻辑。
- 特殊点位规则只在对应关卡的时间与坐标匹配时生效。

- 自动游玩尚未完成实际点击时，遮罩显示 0 进度，避免音乐时间提前移动计划游标造成起点进度误报。
- 遮罩增加完成数量、百分比和可视进度条，进度按已完成时间点计算。

## 0.4.35 - 跨高度转向测试

- 日志显示约 70.75 秒的转向点位于水平路径前方、低于角色约 2.9 单位，高度保护导致漏点；仅当该关时间与点位吻合时按水平位置转向。

## 0.4.34 - 起点标记测试

- 这关开始时 0.051 秒的引导框位于角色起点，日志确认点击会立即错误转向。时间表及坐标吻合时略过该起点标记。

## 0.4.33 - 终点直行测试

- 用户确认该关开启“跳过最后一个点击”后可通关；识别到此关完整时间表且末尾目标在前方直线上时，自动略过最后一个引导框。

## 0.4.32 - 落点直行测试

- 撤回上一版 62.755 秒落地补点点击。新日志证实点击后转向左侧并掉落。
- 当时间、落点坐标及下一引导框的直行方向同时匹配时，略过该落点框，继续等待下一处正常转向。

## 0.4.31 - 落地转向测试

- 对日志确认的 62.755 秒空中经过、落地时已越过约 1 单位的引导点，在同关卡时间与坐标匹配时允许落地立即转向。

## 0.4.30 - 社区版路线修正测试

- 从 Steam 版移植上下层同时间引导点的按高度选择。
- 对已记录的飞行路线、开局缺失转向和异常起点引导点，只有时间与世界坐标均吻合时才应用修正。不同版本关卡坐标是否一致仍需实测。
- 保留社区版已有的角色皮肤、时钟校准、转场与进度续玩逻辑。

## 0.4.29 - 正式版

- 新增全局 **F6** 快捷键：识别当前关卡；与既有 **F7** 启动/恢复、**F8** 停止组成完整快捷键流程。
- 正式发布包同时整理社区版与 Steam 版，且不包含游戏本体、DLL、存档、日志或第三方程序。

## 0.4.28 - 遮罩前台修正

- 遮罩不再每帧重复置顶；仅在首次显示或跟随到新位置时调整窗口层级，避免干扰工具窗口切到前台。
- 游戏进程查找降为每秒一次、关卡状态检查降为每 200 毫秒一次；保留按键图标的高频刷新。
- 工具窗口被激活时立即隐藏遮罩，便于从游戏窗口一次点击切回工具。

## 0.4.27 - 遮罩稳定显示测试

- 遮罩改为固定信息结构：状态、进度与 F7/F8 提示不再随内部日志变化，避免录制时出现短暂杂项文字。
- 点击反馈改为仅变化右侧单个图标：实际按键按下时显示黄色菱形，其他时间保持圆点；不再因点击而整行文字闪烁。
- 移除遮罩中重复的 F8 提示。

## 0.4.26 - 遮罩提示测试

- 游戏内自动游玩状态遮罩改为默认开启，便于录制时明确展示自动游玩状态。
- 关闭遮罩前会提示：若录制或分享自动游玩内容，应在画面或说明中明确标注，避免被误认为手动操作；取消确认将保持遮罩开启。

## 0.4.25 - 遮罩显示优化

- 遮罩改为深色半透明底、青色标题、高对比度状态文字，降低与关卡色彩混淆的情况。
- “正在按下转向”直接读取自动按键的实际按下状态；遮罩刷新间隔调整为约 60 FPS，使提示与游戏内点击同步。

## 0.4.24 - 遮罩编译修正

- 启动脚本在编译遮罩代码时显式引用 `System.Windows.Forms` 与 `System.Drawing`，修复 PowerShell `Add-Type` 找不到 `System.Windows.Forms` 而无法启动的问题。

## 0.4.23 - 游戏内遮罩测试

- 新增可开关的游戏内状态遮罩，显示自动游玩运行状态、当前提示、引导点数量和 F7/F8 快捷键。
- 遮罩为独立透明、鼠标穿透窗口，只在游戏处于前台时显示；关闭选项后立即隐藏，不修改游戏画面或游戏文件。

## 0.4.22 - 快捷键测试

- 新增全局 **F7** 快捷键，用于在已识别当前关卡后启动或恢复自动游玩。
- 保留 **F8** 作为立即停止自动游玩的快捷键；F7 按住时只触发一次，避免重复启动线程。

## 0.4.21 - 路线恢复测试

- 同一关卡中停止自动游玩、手动绕路后再次启动时，改为读取角色当前位置并在整条引导路线中寻找最近的有效提示点。
- 若最近提示已在角色身后，会从其后一个点继续；避免仅沿用旧索引而等待已经远离的提示点。
- 找不到合理的近距离路线点时保留旧索引，避免在转场或非路线区域猜测输入。

## 0.4.20 - 中途恢复测试

- 同一关卡内停止后再次启动自动游玩，会保留停止前的下一个提示点，不再按已流逝的时间强行跳到更后面的点。
- 首次启动或重新识别后仍按当前游戏时间初始化。

## 0.4.19 - 终点选项测试

- 将“跳过最后一个直行终点框”改为手动选项，默认关闭。
- 对于会自动转 45° 进入终点的关卡，请保持关闭；仅在确认最后一个框不用点时开启。

## 0.4.18 - 终点提示点测试

- 最后一个提示点若位于角色当前直线路径上，不发送按键，避免终点展示框被误当作转向指令。
- 仅对最后一个点生效；存在明显横向偏移的终点转向仍按原逻辑处理。

## 0.4.17 - 转场衔接测试

- 检测到角色发生大范围传送后，如当前提示点已处于角色身后且仍在原路线轴线上，跳过该传送过渡点并继续用下一点定位。
- 避免转场自动完成过渡、而脚本仍等待旧提示点造成超时。

## 0.4.16 - 异常提示点过滤测试

- 对于相隔不到 0.08 秒、但空间坐标相距超过 5 个单位的提示点，只过滤后一个异常点。
- 用于排除部分自定义关卡在关卡起点留下的残留标记；正常的密集双转向仍会保留。

## 0.4.15 - 时间点兼容测试

- 将“时间点过密”的拒绝阈值从 0.04 秒调整为 0.03 秒，避免 0.03999996 秒这类单精度误差被误判。
- 仅影响关卡识别；间隔小于 0.03 秒的点仍会拒绝自动游玩并要求导出日志。

## 0.4.14 - 补充转角测试

- 在已校准路线中，如下一已识别提示仍未到时间、却位于角色前方不远且横向距离很大的位置，补发一次转向但不消耗该提示点。
- 该规则仅用于疑似缺失的连续转角（横向距离至少 12 个单位），避免影响常规路线与普通关卡。

## 0.4.13 - 校准路线测试

- 若已确认角色进度与音乐时钟相差约 1 秒，整条路线的横向转角均按世界坐标抵达情况触发，不再仅限开局前几个点。
- 每个点仍有 2 秒的最长等待保护；没有检测到此时钟差的普通关卡继续使用原有 0.5 秒时间窗口。

## 0.4.12 - 开局连续转角测试

- 对已启用开局时钟校准的关卡，前 6 个、3 秒以内的提示点允许额外等待至实际世界坐标的转角。
- 仍须满足横向转角且角色已抵达转角附近才发送按键；其他关卡、后续路段均不受此放宽影响。

## 0.4.11 - 起始转角测试

- 仅针对第一个、时间早于 1 秒、且提示点位于起始段横向的特殊转角，改为在角色实际抵达转角时触发。
- 保留其余引导点的时间窗口、方向和横向对齐保护，避免将一般路线误判为可点击点。

## 0.4.10 - 开局时钟校准测试

- 当角色实际进度与音乐时钟在开局相差 0.5 至 2 秒时，自动使用角色进度校准时间表。
- 修复部分关卡在第一处转向前音乐时钟落后约 1 秒、脚本来不及按键而直接撞墙的问题。
- 普通关卡的两种进度相近时不改变原有时间逻辑。

## 0.4.9 - 怀旧皮肤兼容测试

- 将 `GameCharacterClassic`（怀旧皮肤的实际角色类型）作为受支持的玩家角色。
- “等待绑定”日志新增角色类型与所属关卡地址，便于区分角色类型不兼容和路线来源不匹配。
- 保留玩家与引导点所属关卡必须一致的安全校验，避免对皮肤演示或其他场景误发送按键。

## Steam 版 0.2.9
- 新增独立 `steam/` 目录，不与社区版 0.4.x 共用内存偏移或文件校验。
- 关卡识别优先通过 `GameCharacter` 取得当前关卡；角色尚未绑定关卡时，改由 Steam 版关卡注册表读取 `LevelBase`。
- 用户确认可在进入关卡后识别，无须先使角色开始移动。
- 保留默认开启引导线的可选开关与“实际看到路线后才可自动游玩”的提示。
- 未纳入此前会导致进关闪退的运行时订阅跳板。

## 0.4.8（单独保存，未实测）
- F7 暂停自动点击，手动回到路线后再按 F7 尝试匹配前方引导点。
- 匹配失败保持手动，F8 完全停止；尚未在 Windows 游戏环境验证。
- 不包含自动吃钻石或路线录制。

## 0.4.7.1
- 刚落地后 0.5 秒内，当前目标的横向容差由 1.0 放宽至 1.05；其余转向条件保留。
- 日志显示约 135.28 秒偏差 1.0084 被旧上限阻挡；修复后用户确认该关卡通关。
- 此改动未合入单独存放的 0.4.8 实验快照。

## 0.4.7
- 按键提前量可调，默认 20 毫秒，严格路段可尝试 5 毫秒。
- 用户确认 the feded original 通关。

## 0.4.6
- 增加跳过直行落点提示的实验选项，结合落地状态及下一点方向判断。
- 用户确认迷度 Maxline 的落点误点击问题修复。

## 0.4.5
- 引导点暂未对齐时，在时间窗口内等待，而不是立即判定偏离路线退出。
- 用户反馈足球关卡成功通关。

## 0.4.4
- 疑似传送检测与短暂等待。
- 日志达到容量后保留最新内容，不再停止记录。

## 0.4.3
- 飞跃时保留待执行引导点，等待落地。
- 用户反馈数学关卡成功通关。

## 0.4.2
- 加入按世界坐标触发转向的实验逻辑。

## 0.4.0–0.4.1.1
- 定位 Unity Transform 结构，加入世界坐标诊断。
- 清除残留字段导致的 Add-Type 编译警告。

## 0.3.9
- 将引导点识别与玩家绑定分开，改进首次进入及切关流程。

## 0.3.x
- 时间表扫描、模拟空格、状态日志及时间校准实验。
- 修正固定遍历深度导致的引导点遗漏。
- 用户反馈钢琴关卡通关。

## 0.2
- 用户验证免费引导线与开关功能。
